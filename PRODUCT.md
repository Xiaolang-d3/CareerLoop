# 产品说明

<!-- impeccable:product-schema 1 -->

## 平台

web

## 用户

BossCopilot 面向正在主动求职的个人用户，不限定岗位类别。用户希望减少针对每个岗位重复整理、判断和准备材料的工作，更关注岗位适配、投递质量和准备效率，而不是批量海投。

当前产品是本机单用户工具，不提供多账号或多租户能力。

## 产品目标

BossCopilot 是一个本地优先的个人求职 Agent 工作台。它帮助用户把自己的求职画像、简历、偏好和主动提供的岗位信息组合成一条可持续推进的工作流：

1. 导入并确认岗位信息。
2. 判断岗位是否值得投入。
3. 从真实简历中检索相关证据。
4. 创建岗位定制简历。
5. 准备面试问题、回答素材和行动清单。
6. 记录投递、面试和后续进展。

产品成功意味着用户面对一个具体岗位时，能够更快形成可信的判断和可执行的下一步，同时始终掌握数据、内容和外部操作的最终控制权。

## 产品定位

BossCopilot 的核心机制是把“用户主动提供的岗位上下文”与“本地保存并默认脱敏的简历证据”结合，生成可解释、可纠正、可持续编辑的岗位级结果。

它不是招聘平台爬虫、批量投递工具或只输出一次性文本的聊天机器人。岗位分析、简历版本、面试准备和进度记录都围绕同一个持久化岗位项目组织；系统保留原始证据和用户反馈，不把模型生成内容伪装成已经发生的事实。

## 使用场景

用户通常在招聘网站或其他公开渠道发现岗位，然后主动向 BossCopilot 提供公开 HTTPS 链接、JD 文本或岗位截图。

典型工作材料包括：

- 候选人画像、求职偏好和屏蔽条件。
- PDF、DOCX、TXT 或 Markdown 简历。
- 公开岗位链接、岗位描述和岗位截图。
- 岗位分析、定制简历版本和导出文件。
- 面试准备包、面试轮次和岗位时间线。
- 用户明确开启时获取的公司公开信息及来源。

用户在 BossCopilot 中完成分析和材料准备，自行前往招聘平台完成沟通、发送简历或投递，并回到工作台记录真实结果。

## 能力与约束

- 本地优先保存候选人画像、岗位项目、对话、分析结果、简历版本、面试准备和进度记录。
- 简历默认经过本机隐私扫描；提供给模型的上下文默认使用脱敏文本。
- 模型服务仍可能收到用户授权的脱敏内容，因此“本地优先”不等于“完全离线”。
- 公开岗位链接只允许标准 HTTPS 地址；解析不携带登录状态、Cookie 或账号凭据。
- 页面需要登录、拒绝公开访问或无法直接读取时停止解析，并提示用户改用截图或粘贴 JD。
- 不登录招聘平台、不读取 Cookie、不绕过验证码或安全验证、不逆向私有接口。
- 不自动向招聘方发送消息、附件或投递申请；外部平台上的最终操作由用户完成。
- 用户确认后才保存导入内容或修改本地记录。
- 模型结论必须与原始输入、简历证据和用户反馈区分；不得虚构经历、投递结果或外部事实。
- 联网公司研究是可选能力，默认关闭，并要求保留可点击来源。
- 当前技术形态是 React + TypeScript 前端、FastAPI 后端和 SQLite 本地数据库组成的模块化单体 Web 应用。

## 品牌承诺

- 产品名称为 **BossCopilot**。
- “Copilot”表示辅助用户判断、准备和推进，而不是替用户接管招聘平台账号或作出最终决定。
- 对能力、限制、失败原因和数据流向保持透明。

## 现有依据

仓库中已有可运行的产品实现、自动化测试和以下事实来源：

- `README.md`：当前能力、架构、运行方式和安全边界。
- `docs/current-project-overview.md`：当前产品范围与完整工作流说明。
- `docs/technical-architecture.md`：系统架构、数据和工具边界。
- `frontend/src/main.tsx` 与 `frontend/src/components/WorkspaceViews.tsx`：主要用户界面与任务流程。
- `backend/app/`：岗位、分析、简历、面试、对话、隐私和公开信息研究的实现。
- `backend/tests/`：当前行为与安全边界的自动化测试。

目前没有经过确认的客户案例、公开用户规模、转化率、投递成功率或招聘结果数据。未来界面和文案不得虚构这些证明材料。

## 产品原则

1. **用户保留控制权。** 保存、采用生成内容以及所有招聘平台外部操作都由用户确认。
2. **证据优先于生成。** 岗位判断、简历修改和面试建议尽可能回到真实简历与岗位要求。
3. **隐私与最小权限。** 默认本地处理和脱敏，只在完成当前任务所需的范围内开放数据与工具。
4. **一个岗位，一条连续工作流。** 分析、简历、面试和进度围绕同一岗位项目持续推进，而不是产生相互断裂的结果。
5. **失败时诚实降级。** 无法读取网页、缺少证据或能力未启用时明确说明，并提供截图、粘贴文本或人工补充等替代路径。
