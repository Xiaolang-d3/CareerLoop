# 更新日志

本文件记录 CareerLoop 的用户可见变更。版本号遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)，条目结构参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [Unreleased]

### 新增

- 桌面端三栏应用壳：左侧导航、中间主内容、右侧常驻 AI 问答面板；`#/chat` 聚焦对话而不再独占整页。
- 左侧中文导航对齐参考 IA：首页、我的知识库、AI 问答、知识整理、内容创作、灵感笔记、回顾与复盘、知识图谱、智能工具、设置；后四项与知识整理在无后端时提供诚实占位页。
- 首页仪表盘：问候、四宫格快捷操作、知识概览、最近添加、最近对话、进行中任务与今日灵感；有数据时接真实 API，否则显示空状态。

### 变更

- 默认落地路由由对话改为首页（`#/home`）；浅色侧栏与 `#6557dc` / `#f7f6fb` 色板对齐参考稿视觉。
- 移动端在进入 AI 问答时仍全屏展示对话，避免比现有体验更差。

### 工程


- CI 拆成后端测试、前端单测/构建与 E2E 三个作业，并用 `ci-gate` 汇总结果；E2E 失败会上传 Playwright 轨迹。
- 推送 `v*` 标签时运行 Release workflow：先复用 CI，再打包前端 `dist` 并创建 GitHub Release。本地优先应用不做远程部署。
- PR 若改动 `backend/app/agent` 或 `backend/app/tools`，CI 要求同一变更包含 `docs/agent.md`。分支保护应把 required check 设为 `ci-gate`。

### 变更

- 未配置模型 API Key 时，`GET /agent/capabilities` 不再返回 500，而是给出 `configured: false` 与设置引导；用户可先浏览资料库/工作台/首页，再到设置页配置模型后再对话。

- 产品导航收成「首页 / 证据 / 简历 / 对话」。证据进入原职业画像页（`#/settings/profile`，并增加 `#/evidence` 别名），简历进入定制简历（`#/workbench/resume`）；账号、模型与设置仍从身份菜单进入。
- 首页主 CTA 改为待确认/今日证据；岗位推进和机会中心不再作为首页主入口，项目区块按证据链展示，不再把用户送进独立工作室首页。
- 面试入口合并到项目证据上的同一扇门：工作台「面试问答」与 `#/projects` 面试准备索引会进入项目证据面试；旧的深层哈希仍可打开。
- 项目工作室拆分为总览、架构拆解、材料与分析、面试准备四个子页面，并增加项目列表、项目切换与原项目外链。
- 简历导入会拆分粘连的工作与项目经历，并在进入画像前移除姓名、电话、邮箱、证件号及常见联系方式。
- 项目工作室只在简历内容变化时重建本地证据索引，移除隐私扫描的重量级模型加载，缩短首次和重复打开耗时。

### 移除

- 机会中心不再作为核心模块：侧栏与首页不再推广；`#/opportunities` 与 `#/opportunities/new` 回到首页。岗位队列等旧深层链接仍可用。
- 产品侧栏不再放置「分析」「项目」「设置」。
- 清理从未接线的周报功能（`career_weekly_reports` 表与生成逻辑）、职业洞察与投递追踪接口（`/career-insights`、`/interviews/{id}/debrief`、`application_stage_events` 表），以及 v1 遗留的候选人事实空表。相关功能均无前端入口且从未产生数据；对话工具 `record_interview_debrief` 不受影响。数据库通过 drop 迁移（17–19 号）自动清理，无需手动操作。

## [2.0.0] - 2026-08-24

当前版本为开发预览版，尚未发布稳定的 GitHub Release。

### 新增

- 职业画像、证据账本、待确认事实与多用户本地工作区。
- 机会中心、岗位 A–G 评估、岗位比较和推进记录。
- 简历工作室、项目解析、面试问答、知识点回顾与复盘记录。
- 从公开 GitHub 仓库生成项目 briefing 的项目工作室。
- 受控求职 Agent 的任务路由、计划约束、引用校验、等待恢复和一次同车道重规划。

### 变更

- 产品品牌由 BossCopilot 统一为 CareerLoop，同时保留旧数据库和浏览器偏好的兼容迁移。
- 联网研究改为可选的独立 AgentSearch 服务，并按 general、news、company 场景选择策略。
- 应用版本、API 元数据和网络请求 User-Agent 统一为 2.0.0。
