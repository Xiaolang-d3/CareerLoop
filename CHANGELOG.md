# 变更日志

本文件记录重要的用户可见变更和架构调整，详细开发历史以 Git 提交记录为准。

## 未发布

### 功能

- 默认把前后端开发服务限制在 `127.0.0.1`，不再通过一键启动脚本自动暴露到局域网。
- 将运行时数据源标识从遗留的 `boss` 调整为 `manual`，移除 `JOB_PLATFORM` 配置。
- 修复 SQLite 连接未关闭问题，增加 WAL、忙等待超时以及数据库和本地附件的私有文件权限。
- 修复脱敏模式下简历差距证据可能包含原始个人信息的问题。
- 修复附件解析文本能够影响工具权限路由的问题，权限判断现在只使用用户原始指令。
- 修复 AG-UI 把模型或工具失败标记为正常完成的问题。
- 删除对话时同步清理附件对象，删除岗位或清空简历时同步清理本地知识索引。
- 移除旧版聊天写入、自定义 SSE、原始画像、偏好、岗位、评分和投递创建接口；新消息统一通过 `/ag-ui`，岗位统一通过用户确认的导入流程进入系统。
- 将产品边界调整为“用户主动导入岗位、本地分析和本地记录”，移除浏览器控制、BOSS 页面读取和自动恢复搜索能力。
- 增加用户确认的岗位文字导入和本地截图文字识别，未确认内容不会写入岗位库。
- 增加候选人画像、简历解析、隐私扫描、脱敏模式和本地技能提取。
- 增加私有附件存储、本地或 MinIO 后端、按轮图片直传授权和附件生命周期接口。
- 增加本地知识分块、`sqlite-vec` 检索、岗位分析、简历差距分析、沟通草稿、待投递队列和求职进展工具。
- 增加多对话、任务隔离、上下文截断、早期消息摘要和 Agent 人设与记忆设置。
- 重构聊天工作区为独立 React 组件，增加安全 Markdown/GFM 渲染、回答复制、重新生成、失败重试和可中断的前端请求管理。
- 增加端到端 SSE 流式回答、实时 Agent 工具事件、单对话运行保护和可持久化的真实停止生成流程。
- 接入 assistant-ui ExternalStoreRuntime 与无样式交互原语，统一消息发送、编辑、复制、重新生成、停止和 Composer 状态，同时保留现有 SSE、SQLite、工具事件与视觉设计。
- 增加对话消息回退接口；编辑或重新生成会从目标用户消息重建上下文，避免重复追加同一轮消息。
- 新增标准 `/ag-ui` HTTP/SSE 端点，使用 AG-UI 官方 Python SDK 校验 `RunAgentInput` 并编码生命周期、文本、推理、工具和状态事件。
- 前端改用 AG-UI 官方 `HttpAgent` 消费事件流，移除自定义 SSE 分帧、旧消息写入接口和事件名解析，并将 SDK 延迟加载以控制首屏体积。
- 增加平台无关的 Agent、岗位、模型与工具领域模型。
- 增加模型提供商、招聘平台和工具注册机制。
- 增加 Fake Model Provider 与 Mock Job Platform。
- 将普通聊天消息接入最小 Agent 工具调用循环，并在前端展示调用状态。
- 增加 BOSS 只读平台适配器，支持会话、登录检测、岗位搜索与详情标准化。
- 将 Playwright 固定到单一浏览器线程，并将登录失效和安全验证转换为阻塞状态。
- 增加聊天请求级招聘平台切换，前端可直接选择模拟平台或 BOSS 直聘。
- BOSS 搜索结果按来源链接去重保存到 SQLite，Mock 数据不会写入真实岗位库。
- 增加不依赖 OpenAI 的确定性岗位匹配排序，并在聊天结果中展示匹配分数。
- 重构前端信息架构，增加岗位工作台、Agent 运行状态、快捷搜索、结构化工具执行记录和响应式布局。
- 增加 OpenAI 兼容模型适配器，支持自定义 Base URL、Chat Completions 工具调用和运行时模型切换。
- 移除 Fake 模型备用路径；模型认证、限流、超时和连接异常会返回结构化失败并在前端展开告警。
- 移除 Mock 招聘平台，运行时数据源固定为 BOSS 直聘；登录或页面阻塞时直接告警，不使用模拟岗位替代。
- 将 Playwright 启动失败、组件缺失和操作超时统一转换为 BOSS 平台阻塞告警。
- 增加 BOSS 登录暂停与恢复流程：未登录时提醒用户，确认登录后自动恢复最近一次搜索任务。
- 前端自动轮询 BOSS 登录状态，检测成功后调用恢复接口继续任务；用户无需手动回复确认。

### 文档

- 将 README、技术架构、实施路线、开发环境、模型架构和产品需求文档统一为中文。
- 统一文档中的当前产品边界：系统不访问或控制招聘网站，只处理用户主动提供并确认的内容。
- 确定架构优先的模块化 Agent 建设方向。
- 定义与模型提供商、招聘平台无关的模型、平台和工具边界。
- 定义分阶段实施路线与 Git 协作流程。
- 记录支持的编辑器选择和本地开发环境配置。
